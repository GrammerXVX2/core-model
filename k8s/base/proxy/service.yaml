apiVersion: v1
kind: Service
metadata:
  name: ollama-proxy
  namespace: core-model
spec:
  type: ClusterIP
  selector:
    app: ollama-proxy
  ports:
    - name: http
      port: 11434
      targetPort: http
      protocol: TCP
